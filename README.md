copywriter.mjs: Writes the text.

art-director.mjs: Reads the text, maps out the visuals.

generate-audio.mjs: Reads the text, makes the MP3.

generate-transcript.mjs: Reads the MP3, makes the timestamps.

generate-visuals.mjs: Reads the visual map, downloads the images.